import { createRouter as nativeCreateRouter } from './create-router';
import { prefix as nativePrefix } from './prefix';

export const createRouter = nativeCreateRouter;
export const prefix = nativePrefix;
